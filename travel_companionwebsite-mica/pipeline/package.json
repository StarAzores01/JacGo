{
  "name": "jacgo-data-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Offline data-ingestion pipeline for JAC Go. Pulls POIs and accommodations from OpenStreetMap (Overpass) and enriches them from Wikidata/Wikipedia, then normalizes, dedupes, and exports static JSON that the (static, backend-free) site can load. Not part of the shipped website — this runs on your machine, not in the browser.",
  "type": "commonjs",
  "main": "run.js",
  "scripts": {
    "start": "node run.js",
    "run": "node run.js"
  },
  "engines": {
    "node": ">=18"
  }
}
